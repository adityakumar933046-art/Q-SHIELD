# Contributing to Q-SHIELD

## Development Workflow

1. Fork & clone repository.
2. Create feature branch (`git checkout -b feature/qds-optimization`).
3. Run tests before committing (`bash scripts/run_tests.sh`).
4. Ensure all master architecture files from Phase 0 are preserved.
